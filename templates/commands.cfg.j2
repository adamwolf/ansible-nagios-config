{% for command in nagios_commands %}
define command {
  command_name  {{command.name}}
  command_line  {{command.command}}
}
{% endfor %}

define command {
    command_name check-http-alive
    command_line $USER1$/check_http -H $HOSTADDRESS
}
